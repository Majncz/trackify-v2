/** Shared focus for fields: soft glow + border — no thick ring or offset jump. */
export const focusControl =
  "transition-[border-color,box-shadow] focus-visible:outline-none focus-visible:border-foreground/25 focus-visible:shadow-[0_0_0_3px_hsl(var(--ring)_/_0.07)]";
